export const chartProps = {
    tooltip: {
        context: {
            mode: "quadtree",
            radius: 100
        },
        root: {
            motion: "none"
        }
    },
    highlight: {
        motion: "none"
    }
} as const;
